import { type MRT_TableInstance } from '../types';
interface Props<TData extends Record<string, any> = {}> {
    isSubMenu?: boolean;
    table: MRT_TableInstance<TData>;
}
export declare const MRT_ShowHideColumnsMenu: <TData extends Record<string, any> = {}>({ isSubMenu, table, }: Props<TData>) => import("react/jsx-runtime").JSX.Element;
export {};
